int findNumbers(int* nums, int numsSize) {
    int count[numsSize];
    for(int i=0;i<numsSize;i++){
        count[i]=0;
    }
    int ans=0;
    for(int i=0;i<numsSize;i++){
        while(nums[i]>0){
            count[i]++;
            nums[i]/=10;
        }
    }
    for(int i=0;i<numsSize;i++){
        if(count[i]%2==0){
            ans++;
        }
    }
    return ans;
}
